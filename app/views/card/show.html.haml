%label 登録クレジットカード情報
%br
= "**** **** **** " + @default_card_information.last4
%br
- exp_month = @default_card_information.exp_month.to_s
- exp_year = @default_card_information.exp_year.to_s.slice(2,3)
= exp_month + " / " + exp_year
= form_with(url: "destroy", method: :delete, name: "inputForm", local: true) do |f|
  = f.submit "削除する"
= link_to "商品へ", action: "index", controller: "purchase"